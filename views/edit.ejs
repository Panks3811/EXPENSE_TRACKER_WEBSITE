<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Edit Expense</title>
</head>
<body>
    <div class="container">
        <h1>Edit Expense</h1>
        <form method="POST" action="/update/<%= expense.id %>" autocomplete="off">
            <div class="form-group">
                <label for="expense_name"><b>Expense Name:</b></label>
                <input type="text" id="expense_name" name="expense_name" class="form-control" value="<%= expense.expense_name %>">
            </div>
            <div class="form-group">
                <label for="amount"><b>Amount:</b></label>
                <input type="number" id="amount" name="amount" step="0.01" class="form-control" value="<%= expense.amount %>">
            </div>
            <div class="form-group">
                <label for="date"><b>Date:</b></label>
                <input type="date" id="date" name="date" class="form-control" value="<%= expense.date %>">
            </div>
            <input type="submit" name="update" value="Update Expense" class="btn btn-primary">
        </form>
    </div>
</body>
</html>
